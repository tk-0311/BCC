Program library

Namespace

Initialize struct Stack
  set int array[15] initialize with NULL values;
  set int top -1
  Start contructor with no argument
    Start For loop int i from 0 to 14
      array at i index to null
    End For loop
  End constructor
End struct stack

set function push
set function pop
set function top
set function print
set function validation for integer
set function validation for char

Start main function
  initialize option variable to empty string
  while option is not E or not e
    Initialize stack  
      While option is not D or E
      invoke validation parameter opt
      Switch option
        case A push
          invoke push function parameter stack
          end case
        case B pop
          invoke pop function
          end case
        case C top
          invoke top function
          end case
      End Switch
      invoke print function
    End while loop
  End while loop
End main

Start push function argument reference of stack
  if the top is less then 14;
    print string "Enter a number between 0 and 99 to push to the stack"
    invoke validation function save return value
    increment top by one
    save to returned value from validation into stack at top index
  else print "stack is full"
End push function

Start pop function argument reference of stack
  if top is bigger then -1
    pop value at top index
    decrement top by one
  else print "Error underflow, stack is Empty"
End pop function

Start top function
  if top is bigger then -1
    print stack at top index
  else print "there is no element in stack
End pop function

Start print function argment reference of stack
  print ---
  for loop from i = 0 to i is less then top increment by one
    print "|" + stack at "|"
  End for loop
  print ---
End print function

Start validation function no argument
  while loop until break;
  cin value from a user
  if cin value is not integer ask again
  if correct return the value
  print "Wrong input try again"
  End while loop
End validation function 

Start validation function argument char opt reference
  while loop until break
    cin opt
    if (utf char num is larger then 64 and less then 70)
    return opt
    if larger then 96 and less 101
    return opt - 32
    print "Wrong input try again"
  End while loop 
End validation function 

github link https://github.com/tk-0311/BCC